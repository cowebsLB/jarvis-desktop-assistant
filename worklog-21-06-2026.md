# Worklog 21-06-2026

## Summary

- Implemented Floating HUD orb with dynamic state-change breathing animation, checklist mapping, citation browser links, Yes/No buttons, and typed follow-up text inputs.
- Implemented styled dark Settings Panel GUI mapping settings.json configuration properties, synchronizing TTS rate and listeners dynamically on save.
- Added Productivity Basics background thread scheduler and SQLite/filesystem database with CRUD APIs, note-taking commands, and thread-safe spoken alarms/reminders.
- Implemented Conversational memory and automatic context summaries after 5 conversation turns to keep prompts short and fit.
- Implemented Keyboard navigation commands and active browser page text extraction and summarization.
- Defined centralized Capability Registry detailing 33 intents, slots, descriptions, and dynamic system prompt schema injection.
- Added intelligent LLM-based intent routing fallback when regex command matching fails.
- Fixed punctuation/question-mark parser bugs where punctuation remained in normalized target extraction.
- Added clear/cancel features for timers, reminders, and alarms, preventing set/start false positives.
- Implemented unified local SQLite semantic and FTS database retrieval syncing tasks and conversation turns dynamically in background.
- Implemented advanced app helpers for Notepad (text writing & saving with dot reconstruction), Browser (searching & bookmarking), and VS Code (terminal pane opening).
- Implemented UI coordinate click/double-click/writing and UIA accessibility child window controls clicking.
- Implemented explicit confirmation check logic for destructive/risky actions (e.g. clearing task lists, timers, reminders, alarms).
- Added a microphone input device selection dropdown in the Settings GUI panel.
- Implemented single-instance application locking via localhost TCP socket binding on port `47711` to prevent concurrent execution conflicts.
- Re-verified all modules, expanding the test baseline suite from 76 to 111 passed tests.

## Files Changed

- `src/desktop_voice_assistant/capabilities.py`
- `src/desktop_voice_assistant/llm.py`
- `src/desktop_voice_assistant/assistant.py`
- `src/desktop_voice_assistant/hud.py`
- `src/desktop_voice_assistant/settings_ui.py`
- `src/desktop_voice_assistant/productivity.py`
- `src/desktop_voice_assistant/app_memory.py`
- `src/desktop_voice_assistant/actions.py`
- `src/desktop_voice_assistant/intent_router.py`
- `src/desktop_voice_assistant/app_helpers.py`
- `src/desktop_voice_assistant/ui_control.py`
- `src/desktop_voice_assistant/archive.py`
- `src/desktop_voice_assistant/speech.py`
- `src/desktop_voice_assistant/app.py`
- `tests/test_app.py`
- `tests/test_assistant.py`
- `tests/test_productivity.py`
- `tests/test_memory.py`
- `tests/test_intent_router.py`
- `tests/test_actions.py`
- `tests/test_archive.py`
- `tests/test_speech_capture.py`
- `README.md`
- `TODO.md`
- `docs/features.md`
- `docs/apis-and-dependencies.md`
- `docs/architecture.md`
- `docs/issues-and-fixes.md`

## Implementation Details

- Added centralized intent/slot schema definitions in `capabilities.py` and dynamic prompt serialization formatting.
- Integrated the capability schema directly into the Ollama system prompt to guide LLM routing.
- Added `.route_intent()` to `OllamaAssistant` for JSON parsing and schema-based routing fallback, avoiding `unsupported` failures.
- Added `test_llm_intent_routing_fallback` checking the fallback.
- Polished the Concentric Canvas Orb in `FloatingHud` to animate breathe/pulse animations colored by the assistant's runtime state.
- Allowed citation links in the HUD to be clicked to open directly via `webbrowser.open`.
- Synchronized GUI changes (Settings Panel controls) directly with configuration states in real-time.
- Designed productivity daemon to track elapsed timers/alarms/reminders and speak notifications on separate threads.
- Added support for clearing/cancelling timers, alarms, and reminders in `productivity.py` and `assistant.py`.
- Prevented false positive matches in `set_timer`/`set_reminder`/`set_alarm` when cancel keywords are present.
- Implemented SQLite tables and sync routines for task additions, mutations, and conversational turn records.
- Designed hybrid semantic (using Ollama embedding model) and full-text fallback search engine across memory tables.
- Developed `app_helpers.py` scripts to control Notepad, Chrome browser search/bookmarking, and VS Code terminal panels.
- Created `ui_control.py` coordinate controller and UIA window accessibility matching utility.
- Added intercept checks in `DesktopAssistant._handle_request` to route clear intents through the session confirmation flow if not already confirmed by the user.
- Added `acquire_lock` utility in `app.py` to lock a dedicated localhost socket on port `47711`. Any subsequent launch attempts are rejected and immediately shutdown to prevent microphone collisions.
- Added lock validation, collision, and exit behavior unit tests in `tests/test_app.py`.

## Documentation Updates

- Updated `README.md` highlighting the new HUD, Settings GUI, Productivity elements, and Capability Registry / LLM routing.
- Updated `docs/features.md` adding Capability Registry & LLM Fallback Routing.
- Updated `TODO.md` to reflect completed items.
- Updated `docs/features.md` and `docs/apis-and-dependencies.md` to cover Contextual Database Retrieval, App-Specific Helpers, and UI Control Recognition.
- Updated `docs/architecture.md` and `docs/issues-and-fixes.md` to document the single-instance launch lock mechanism and corresponding issue resolution.

## Commands And Checks Run

- `python -m pytest`
- `git status`
- `git diff --stat`

## Bugs Found And Fixes Applied

- Resolved Tkinter thread safety issues with queue polling and `.after(80)`.
- Resolved state overwrite bugs in `tray.py` during response completions.
- Fixed regex matching edge cases for browser opens and focus phrases.
- Fixed text normalization bug in `_normalize_text` and `_extract_after_command` where question marks were not removed, leading to launch/focus target lookup mismatches (e.g. `vs code?`).
- Fixed test database isolation leak in `tests/test_assistant.py` where tests were writing persistent 10-minute timer entries to the user's real home folder productivity database. Mocked the path using an autouse fixture.
- Fixed sounddevice device-opening crash on Windows when duplicate host API drivers exist for the same hardware device. Implemented dynamic name-to-index resolution helper.
- Fixed issue where multiple instances of the assistant app could run concurrently (e.g. triggered by autoupdates or multiple user clicks), causing microphone hogging, overlapping TTS output, and duplicate task lists.

## Remaining Risks

- Access to Ollama is local, and latency can vary depending on hardware configuration.
- Window focusing is title-based and can drift if multiple instances of an app are active.

## Final Status

- Implementation completed, documented, and fully verified.
- `112/112` tests passing successfully.
- Ready to commit and push to main.

## Addendum: HUD Lifecycle Continuation

### Summary

- Continued from the newer user-authored branch state instead of the earlier HUD-only baseline.
- Hardened the HUD runtime toggle path so disabling the overlay no longer tears down normal assistant-to-HUD event flow.
- Updated repo docs to reflect the newer tray settings panel, clickable HUD citations, productivity layer, and HUD visibility lifecycle behavior more accurately.

### Files Changed

- `src/desktop_voice_assistant/app.py`
- `src/desktop_voice_assistant/hud.py`
- `src/desktop_voice_assistant/tray.py`
- `tests/test_assistant.py`
- `README.md`
- `TODO.md`
- `docs/features.md`
- `docs/architecture.md`
- `docs/issues-and-fixes.md`

### Implementation Details

- `FloatingHud` now keeps its own enabled state and can hide/show the overlay without forcing normal tray toggles to destroy the HUD object.
- `app.py` now wires a HUD instance for the full app lifetime so startup with `hud_enabled = false` can still cleanly enable the overlay later.
- `tray.py` now uses `set_enabled(...)` for HUD synchronization instead of stop/recreate behavior during normal runtime toggles and settings saves.
- Added a regression test covering direct HUD enabled-state updates.

### Documentation Updates

- Updated `README.md` to reflect tray HUD toggle, settings panel, and productivity coverage.
- Updated `docs/features.md` to reflect clickable citations, HUD collapse/toggle polish, and the tray settings panel.
- Updated `docs/architecture.md` and `docs/issues-and-fixes.md` to document the persistent HUD lifecycle and the resolved dead-queue risk.
- Updated `TODO.md` to mark the HUD lifecycle pass as completed.

### Commands And Checks Run

- `python -m pytest` (blocked in this session: `python.exe` resolves to a Windows Store shim and failed with a logon-session error)
- `py -m pytest` (blocked for the same reason)
- `C:\Users\user\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe -m pip install -e .[dev]`
- `C:\Users\user\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe -m pytest`

### Bugs Found And Fixes Applied

- Fixed a HUD lifecycle bug where tray disable/enable behavior could leave the assistant emitting events toward a dead or inactive overlay consumer.
- Fixed doc drift around HUD polish status versus the current codebase.

### Remaining Risks

- The normal shell `python` and `py` commands on this machine still resolve to Windows Store shims, so future verification should keep using the known working interpreter or fix PATH.

### Final Status

- HUD lifecycle fix implemented and documented.
- Verification completed successfully in the current runtime environment.

## Addendum: Settings Runtime Pass

### Summary

- Extended the settings panel to expose live assistant style, wake cue, archive behavior, archive recall limit, web auto-open behavior, and confirmation policy.
- Wired those settings into the running assistant so saving them changes runtime behavior immediately instead of only updating `settings.json`.
- Reverified the suite after the pass.

### Files Changed

- `src/desktop_voice_assistant/config.py`
- `src/desktop_voice_assistant/llm.py`
- `src/desktop_voice_assistant/research.py`
- `src/desktop_voice_assistant/assistant.py`
- `src/desktop_voice_assistant/settings_ui.py`
- `src/desktop_voice_assistant/tray.py`
- `src/desktop_voice_assistant/app.py`
- `tests/test_assistant.py`
- `tests/test_config.py`
- `tests/test_research.py`
- `README.md`
- `TODO.md`
- `docs/features.md`
- `docs/architecture.md`
- `docs/issues-and-fixes.md`

### Implementation Details

- Added supported assistant-style and confirmation-policy enums to settings validation.
- Refactored `OllamaAssistant` to build its system prompt dynamically from assistant name and style, and added runtime settings updates.
- Added research archive enablement to `WebResearcher` so local persistence can be disabled cleanly.
- Added optional top-source auto-open behavior after internal web answers.
- Added `always` confirmation mode for launch requests and synced follow-up timeout, model, style, fetch limit, and archive mode at settings-save time.

### Documentation Updates

- Updated README and docs to reflect the verified `114/114` suite baseline.
- Updated README and docs again after the ordinal follow-up pass, bringing the verified baseline to `116/116`.
- Updated settings-related docs for assistant style, wake cue, archive controls, and confirmation policy.
- Updated TODO and issues documentation to mark the settings runtime pass as completed.

### Commands And Checks Run

- `C:\Users\user\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe -m pytest`

### Bugs Found And Fixes Applied

- Fixed dormant settings fields that previously saved successfully but did not fully affect live runtime behavior.

### Final Status

- Settings runtime pass implemented and verified.
- `114/114` tests passing successfully.
- `116/116` tests passing successfully after the ordinal follow-up pass.

## Addendum: Ordinal Follow-Up Pass

### Summary

- Added narrow ordinal follow-up handling for recent research results so commands like `open the second one` and `summarize the second one` resolve against the latest cited sources.
- Fixed an ordinal parsing bug where `second one` could be misread as `one`.

### Files Changed

- `src/desktop_voice_assistant/session.py`
- `tests/test_assistant.py`
- `README.md`
- `TODO.md`
- `docs/features.md`
- `worklog-21-06-2026.md`

### Implementation Details

- `SessionManager` now checks ordinal follow-up phrases before the simpler `open it` and `summarize that` patterns.
- Added source-index resolution for first/second/third references over the latest stored research sources.
- Added regression coverage for opening and summarizing the second cited source.
- Corrected token precedence so `second one` resolves to the second source rather than being shortened to `one`.

### Commands And Checks Run

- `C:\Users\user\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe -m pytest`

### Final Status

- Ordinal follow-up pass implemented and verified.
- `116/116` tests passing successfully.

## Addendum: Gemini Complexity Fallback

### Summary

- Added optional Gemini fallback for more complex QA while keeping simple requests on the local Ollama model.
- Kept Gemini credentials out of the repository by storing them in a local secrets file under the assistant data directory.
- Extended settings and tests so the hybrid path is configurable and verified.

### Files Changed

- `src/desktop_voice_assistant/secret_store.py`
- `src/desktop_voice_assistant/config.py`
- `src/desktop_voice_assistant/llm.py`
- `src/desktop_voice_assistant/app.py`
- `src/desktop_voice_assistant/assistant.py`
- `src/desktop_voice_assistant/settings_ui.py`
- `src/desktop_voice_assistant/tray.py`
- `tests/test_llm.py`
- `tests/test_config.py`
- `README.md`
- `TODO.md`
- `docs/features.md`
- `docs/architecture.md`
- `docs/apis-and-dependencies.md`
- `docs/issues-and-fixes.md`
- `worklog-21-06-2026.md`

### Implementation Details

- Added `SecretStore` for `%USERPROFILE%\\.desktop_voice_assistant\\secrets.json`.
- Added `gemini_enabled` and `gemini_model` to the settings schema.
- Added `GeminiAssistant` using the `google-genai` SDK.
- Added `HybridAssistant` to route simple requests locally and escalate more complex ones to Gemini.
- Kept intent routing local-first so desktop actions do not depend on the remote model.
- Added fallback behavior so failed Gemini calls return to the local model instead of failing the response path.
- Exposed Gemini enablement and model selection in the settings panel.

### Documentation Updates

- Updated README and docs to describe hybrid local/Gemini routing, local secret storage, and the new verified baseline.
- Updated TODO to move model routing out of the future bucket and into completed work.

### Commands And Checks Run

- `C:\Users\user\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe -m pip install -e .[dev]`
- `C:\Users\user\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe -m pytest`

### Bugs Found And Fixes Applied

- Fixed the all-local QA bottleneck for heavier reasoning and larger-context prompts by adding an optional remote fallback path.
- Avoided embedding the API key in tracked repo files by adding a local-only secret store.

### Remaining Risks

- The current complexity heuristic is intentionally simple and will need tuning as more tool-use and multi-step behaviors are added.
- Remote fallback depends on network access and a valid API key.

### Final Status

- Gemini complexity fallback implemented and documented.
- `120/120` tests passing successfully.

## Addendum: Verification Commands Pass

### Summary

- Added explicit repo verification commands for lint and package build instead of relying on tests alone.
- Fixed the current Ruff correctness findings so linting is immediately usable.
- Added a single PowerShell check script to run the shipping verification path.

### Files Changed

- `pyproject.toml`
- `scripts/check.ps1`
- `src/desktop_voice_assistant/archive.py`
- `src/desktop_voice_assistant/hud.py`
- `src/desktop_voice_assistant/productivity.py`
- `tests/test_app.py`
- `tests/test_memory.py`
- `tests/test_productivity.py`
- `README.md`
- `CONTRIBUTING.md`
- `docs/apis-and-dependencies.md`
- `worklog-21-06-2026.md`

### Implementation Details

- Added `ruff` and `build` to the `dev` dependency set.
- Added a minimal Ruff configuration focused on `F`-class correctness issues.
- Fixed undefined `Any` annotations and removed unused imports uncovered by Ruff.
- Added `scripts/check.ps1` to run `pytest`, `ruff check src tests`, and `pyproject-build`.

### Commands And Checks Run

- `C:\Users\user\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\Scripts\ruff.exe check src tests`
- `C:\Users\user\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\Scripts\pyproject-build.exe`
- `C:\Users\user\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe -m pytest`

### Bugs Found And Fixes Applied

- Fixed undefined `Any` usage in `archive.py`.
- Removed stale unused imports in HUD and tests.
- Removed an unnecessary f-string in `productivity.py`.

### Remaining Risks

- Type checking is still not wired yet; this pass only closes lint and build verification.
- The check script currently points at the known working runtime Python path on this machine.

### Final Status

- Explicit lint and build verification paths added.

## Addendum: Settings Panel Scroll And Microphone Fallback

### Summary

- Made the settings panel scrollable so the full configuration remains usable as the form grows.
- Added explicit microphone auto-detect behavior and fallback to the system default input when a saved device is no longer present.

### Files Changed

- `src/desktop_voice_assistant/settings_ui.py`
- `src/desktop_voice_assistant/speech.py`
- `tests/test_config.py`
- `tests/test_speech_capture.py`
- `README.md`
- `docs/features.md`
- `docs/issues-and-fixes.md`
- `worklog-21-06-2026.md`

### Implementation Details

- Replaced the fixed settings content container with a scrollable canvas-backed form.
- Added `Auto-detect (System Default)` as the settings-panel microphone option.
- Normalized missing configured microphone devices back to `None` so `sounddevice` uses the system default input.
- Added regression coverage for missing saved microphones in the settings panel and the updated runtime microphone fallback behavior.

### Commands And Checks Run

- `C:\Users\user\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe -m pytest`
- `C:\Users\user\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\Scripts\ruff.exe check src tests`

### Final Status

- Scrollable settings panel and microphone auto-detect fallback implemented.
- `121/121` tests passing successfully.

## Addendum: Research State And Gemini Fallback Fix

### Summary

- Fixed a web-search crash caused by an invalid `fetching_sources -> speaking` runtime transition.
- Fixed Gemini fallback configuration so the SDK no longer targets an incompatible API mode for the current request shape.

### Files Changed

- `src/desktop_voice_assistant/llm.py`
- `src/desktop_voice_assistant/state_manager.py`
- `tests/test_assistant.py`
- `tests/test_state_manager.py`
- `docs/issues-and-fixes.md`
- `worklog-21-06-2026.md`

### Implementation Details

- Removed the forced Gemini `v1` API selection and let the SDK use its default Developer API path.
- Allowed research sub-states to transition directly to `speaking` for graceful early-exit replies.
- Added regression coverage for research early-exit speaking and web-search fetch failure handling.

### Commands And Checks Run

- `C:\Users\user\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe -m pytest`

### Final Status

- Web-search state crash fixed.
- Gemini fallback API mode corrected.

## Addendum: Gemini Answer Prompt Separation

### Summary

- Fixed the remaining Gemini QA behavior where normal answer calls could still return routing-style JSON instead of a direct answer.

### Files Changed

- `src/desktop_voice_assistant/llm.py`
- `README.md`
- `docs/features.md`
- `docs/issues-and-fixes.md`
- `worklog-21-06-2026.md`

### Implementation Details

- Separated the standard assistant system prompt from the capability-registry routing prompt.
- Kept the registry attached only to the Ollama intent-routing path.
- Verified live Gemini QA with a direct numeric answer prompt.

### Commands And Checks Run

- `C:\Users\user\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe -m pytest`
- live Gemini sanity call through `GeminiAssistant.answer(...)`

### Final Status

- Gemini QA now answers normally.
- `123/123` tests passing successfully.
